# `starfly-graph` service

> **Status:** Preview — binary export pending.

## Why this exists

Run the runtime identity knowledge graph as a sidecar: subscribe to fabric NATS subjects, write to FalkorDB, expose MCP and REST query APIs. The PEP does not wait for this process.

## Documentation

- [Starfly Graph integrator guide](https://starfly.dev/1.0/docs/integrators/starfly-graph/)
- Library: [pkg/graph](../../pkg/graph/)

## Code

`main.go` and container image not in this export yet. This README reserves the path so docs can link here until the service ships.
